{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug raytracing",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/raytracing",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stdio": [null, "${workspaceFolder}/im.ppm", null],
            "preLaunchTask": "cmake build (Debug)"
        }
    ]
}
